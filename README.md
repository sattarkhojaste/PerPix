
  # Redesign portfolio structure

  This is a code bundle for Redesign portfolio structure. The original project is available at https://www.figma.com/design/MBuF9wxx3D73tmSkxIJME6/Redesign-portfolio-structure.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  